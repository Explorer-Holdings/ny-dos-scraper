# Apify SDK — pulls in crawlee automatically
apify>=2.3.0,<3.0.0

# Pydantic v2 — must be installed before crawlee is imported
pydantic>=2.7.0,<3.0.0

# Playwright for browser automation
playwright>=1.44.0,<2.0.0

# Async HTTP client
httpx[http2]>=0.27.0,<1.0.0
